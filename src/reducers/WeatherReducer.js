export default function(state = [], action){
	switch(action.type){
		case "GET_WEATHER":
			//code will run if action. type = GET_WEATHER
			payload action.payload 
		case "Do the Thing":
			return state;
		default:
			return state; 
	}
}